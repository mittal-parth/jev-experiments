# Required for --policy jev (https://console.typesafe.ai)
# Cursor runtime secret name is JEV_API_KEY; TYPESAFE_API_KEY also works.
JEV_API_KEY=
TYPESAFE_API_KEY=

# Optional override; default is jev-latest
TYPESAFE_MODEL=jev-latest

# Inspectors: Dino-Jev on :8766, Krunker-Jev on :8765
